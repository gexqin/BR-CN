# 原版 bug 修复清单(v2 相对原版 Perl CGI)

审查发现的全部原版缺陷及本重写的处理。未列入者=保持原版行为。

## 安全
1. **玩家命令无鉴权调用管理功能**(battle.cgi:106-114 `Command=RESET/BSAVE/BREAD`
   直接 require admin.cgi 调用子程序)→ 通路彻底移除;管理接口独立鉴权。
2. **明文密码全链路**(每请求 hidden 字段携带、cookie 存整行明文、URL 查询串)
   → pbkdf2 哈希落库 + httpOnly 会话 token。
3. **"死亡后 2 小时禁注册"失效**(CSAVE 恒写 endtime=0)→ 服务端死亡时间生效。
4. serve 阶段的路径遍历/静态文件泄露等 CGI 服务器问题 → 随 CGI 一并移除。

## 战斗
5. **近战负伤掷不出"足"**(attack.cgi `int(rand(3))` 只有 0/1/2,
   `dice4==3` 分支死代码)→ `rand(4)`,足部负伤可出现。
6. **DEFTREAT 三分支引用未定义变量 `$b_kind_b`**(attack.cgi:701-706,
   殴→铠甲 ×1.5、刺→铠甲 ×0.5、刺→锁子甲 ×1.5 永不生效)→ 按设计意图实现。
7. **ATTACK2 敌方命中判定用玩家命中率**(attack.cgi:231 `dice1 < $mei`,
   应为敌方 `$mei2`)→ 攻防双方各用自身命中率。
8. **反击命中复用同一颗骰子**(ATTACK2 玩家反击沿用 `dice1`)→ 各掷新骰。
9. **敌方饰品防御从未生效**(attack.cgi:65/220 `$ball +=` 写错变量,
   应为 `$ball2`)→ 双方对称计算。
10. **NPC 攻击饰品吸收查敌人饰品**(attack.cgi:465 `$w_item[5]` 应为
    `$item[5]`)→ 对称实现。
11. **WNS(双属性刀)命中按斩、熟练度按刺**(TACTGET 与 WEPTREAT 判定序
    不一致)→ 统一按刺系(含相性判定)。
12. **击杀夺物菜单不可用**(lib2.cgi:519 `value="GET_$i]"` 多余 `]`)
    → 修复,击杀/搜尸共用可用菜单。

## 世界/物品
13. **区域攻击修正失效**(br.cgi @arsts 用 AU/AD,判定代码只匹配 WU/WD,
    观音堂/旅馆废墟/废弃学校/南村住宅街 4 地点)→ 码表统一,修正生效。
14. **MOVE"下次禁区"分支跳过当前禁区检查**(battle.cgi:136-147,可移入
    当前禁区)→ 先检查当前禁区。
15. **随机抽取 off-by-one**(`int(rand($#list))` 抽不到最后一项:配发武器/
    私物/区域拾取)→ 统一全列表均匀。
16. **提前 WINEND1 写"结束"标志致游戏僵死**(lib2.cgi:883、item.cgi 陷阱/
    毒死、hack.cgi 颈环引爆:mem==1 即写结束 → 禁区停更 → 正式结局永不可达)
    → 统一由胜负判定(剩余 1 人且 ar>10)收束。
17. **合成素材未投放**(轻油/汽油/火药/导火索/喷雾罐/打火机/苹果电脑不在
    任何 dat,合成/黑客/投毒/扩音器/EX 结局全部不可达)→ 按主题补投
    (tools/migrate_dat.py EXTRA_PLACEMENTS)。
18. **UTF-8 姓名长度误判**(原按字节 8=4 汉字,转码后 3 汉字即超限)
    → 按字符数校验(≤4)。
19. **战斗日志阈值语义变化**(2000 字节在 UTF-8 下提前 ~1/3 截断)
    → 按 2000 字符。
20. **rank.cgi 把普通 NPC 计入剩余人数 / regist 人数上限用标量比较**等
    统计错误 → SQL 精确统计。
21. **news.cgi `/<UL>/` 永不匹配** 等渲染 bug → 时间线重渲染。

## 保留的原版行为(有意不复刻为 bug)
- 每次遭遇只交换一回合,交战锁待对方行动解锁(原版 bid 机制)。
- 击杀者可搜刮一件;尸体发现者可反复搜刮。
- 雷达 R1/R2 渲染一致(原版"简易雷达只显示本区"未实现,保持全图)。
- 黑客解除禁区仅到次日零点。
- 政府 NPC 无 AI,仅被动反击;不计剩余人数。
- 战斗中防具代伤立即生效,武器破坏/负伤仅命中时生效(原版 wep_2 机制)。

## 未移植(记录)
- 图标系统(原版 $icon_mode=0 关闭,img/ 不存在)。
- 基本方针 tactics(原版字段存在但无任何逻辑)。
- 个人存档 u_save / USRSAVE(被管理端整档备份取代)。
- cookie 十六进制全量存档(被服务端会话取代)。

## v2 审查修复(2026-08-19,第二轮全面审查)

安全:
22. **管理密码默认值 `790923`** → 移除默认,未设 `BR_ADMIN_PASS` 时管理登录禁用(503);
    比较改 `hmac.compare_digest`(常数时间)。
23. **登录/管理登录无暴力破解防护** → 内存限速:失败 5 次锁定(登录 10 分钟,
    管理 15 分钟),按 用户名+IP 双键;注册按 IP 20 次/小时。
24. **no_id/wrong_pass 可枚举用户名** → 统一返回 wrong_pass。
25. **会话 cookie 无 Secure** → 新增 `BR_COOKIE_SECURE=1` 开关。
26. **密码上限 8 半角字符**(口令空间过小)→ 放宽至 32(前端/文案同步)。

引擎:
27. **尸体发现率 off-by-one**(`dice4 > 9` 恒假 → 100% 发现)→ 改 `<`,
    恢复 10% 语义;`cmd_attack` 同步收紧:未发现尸体仅 击杀者/发现者/无主 可进。
28. **读-改-写竞态**:player 行在 BEGIN IMMEDIATE 外读取、仅死亡时重读,
    flush 覆盖并发写(丢伤害/变相回血)→ 取得写锁后无条件重读。
29. **cmd_loot 空槽无守卫**(可"拾取"空手/内衣/无名物品)→ 对等原版 WINGET
    空/空手/内衣 放弃拾取。
30. **毒药/AD 饰品 uses=None(∞)被 `(x or 0)-1` 一次耗尽** → None 不扣减。
31. **全员死亡时游戏永久卡 running**(只检查 mem==1)→ mem==0 置
    finished_win(无优胜者)终局。
32. **注册校验与写入非同事务**,并发撞 UNIQUE → 500 → 捕获 IntegrityError
    转友好 dup 提示。
33. **busy_timeout=5000 覆盖 timeout=10** → 统一 10s。
34. **前端投毒/验毒过滤正则过宽**(`/S|H/` 匹配武器)→ 与后端一致 `/SH|HH|SD|HD/`。
35. **感知增量游标死设计**(since_id 恒 0)→ 接入 `/api/game/state?since_id=`,
    前端往返游标,多标签页/刷新不重复计数。
36. 删除死代码:`death.check_level_up`、`util.kind_params`、
    `time_utils.registration_open`(实际逻辑另有实现,防双实现漂移)。
