# BATTLE ROYALE CN 使用说明

> 供管理员(部署/开局)与玩家(游玩)双方参考。技术架构见 [README.md](README.md)。

---

## 一、管理员:部署与开局

### 1. 启动

```bash
pip install --user fastapi uvicorn      # 仅两项依赖(Python 3.10+)
BR_ADMIN_PASS=你的管理密码 python3 run.py 8000
```

- `BR_ADMIN_PASS` **必须设置**,否则管理后台无法登录(这是安全设计,没有默认密码)。
- 启动后访问 `http://127.0.0.1:8000/`;局域网内其他设备访问 `http://<主机IP>:8000/`。
- 数据保存在 `v2/brcn.db`(SQLite),换新档删掉该文件即可。

### 2. 环境变量

| 变量 | 默认 | 说明 |
|---|---|---|
| `BR_ADMIN_PASS` | 无(必设) | 管理密码;连续输错 5 次锁定 15 分钟 |
| `BR_ADMIN_USER` | `admin` | NPC 账号名前缀(仅内部使用) |
| `BR_DB` | `v2/brcn.db` | 数据库文件路径 |
| `BR_COOKIE_SECURE` | `0` | 经 HTTPS 反向代理部署时置 `1` |

### 3. 开局流程

1. 打开 `http://127.0.0.1:8000/#/admin`,输入管理密码登录。
2. 点「数据初始化」开新局:随机禁区顺序、全地图撒物品、政府系 NPC(班主任+士兵)进驻分校。
3. 把网址发给玩家,等待大家在首页「新学员注册」报名。
4. 游戏过程中可随时使用管理功能(见下)。

### 4. 管理后台功能

| 功能 | 说明 |
|---|---|
| 玩家一览 | 全员状态:班级/社团/存活/死因/击杀数/所在地 |
| 处刑 | 强制处决指定玩家(政府处决公告),用于裁决违规 |
| 整档备份 | 快照当前全部数据,可加标签 |
| 回滚 | 恢复到某次备份(会清掉备份点之后的全部进展,慎用) |

> 建议:每个游戏日开始时手动做一次备份,出问题有后悔药可吃。

---

## 二、玩家:游戏指南

### 1. 注册

- **姓名**:姓、名各最多 4 个汉字,不能含英文数字;同姓同名且未死者不能重复注册。
- **ID / 密码**:半角英数,ID ≤ 8 位、密码 ≤ 32 位,ID 不能等于密码。
- **口癖**(杀害对手时的台词)、**遗言**(自己死亡时)、**座右铭**(生存者一览可见):各 ≤ 32 字。
- 注册后随机获得:初始武器、随身物品、社团(影响初始熟练度与部分特殊能力)、班级。
- 每局上限 210 人(男女各 5 班 × 21 人);死亡后 **2 小时**内不能用同一 ID 重新注册。

### 2. 主界面数值

| 数值 | 说明 |
|---|---|
| 体力(HP) | 归零即死亡。移动/探索不扣,战斗受伤、耐力耗尽、毒、禁区处决会扣 |
| 耐力(STA) | 行动消耗;耗尽后强制休息并损失体力 |
| 攻/防 | 基础值 + 装备加成,所在区域另有攻防修正 |
| 负伤 | 头/腕/腹/足四处;头部受伤命中率大降,足伤移动探索消耗加倍;可用应急治疗处理 |
| 熟练度 | 武器类别各一条,越高命中率/攻击倍率越高 |

### 3. 基本行动

- **移动**:去相邻区域,消耗耐力。
- **探索**:同一区域搜索——可能遇到其他玩家(先制/被袭)、发现物品、触发事件、发现尸体。
- **睡眠**:恢复耐力,每 6 秒 +1;**治疗**:恢复体力,每 12 秒 +1。
  恢复不是即时结算:在你**下一次操作或登录时**一并结算。睡觉/治疗期间处于无防备状态,被袭击会按熟睡处理,请找安全的地方。
- **口癖变更**:随时修改口癖/遗言/座右铭。

### 4. 战斗

- 探索遇到其他玩家时:六成先制(你发现对方,获得攻击机会),四成被袭(对方立即进攻)。
- 每次交战只交换一回合;之后进入**交战锁**——对方行动一次之前,你不能对同一目标再次攻击。
  换句话说:两人对打需要交替行动,追杀要一步一步来。
- 攻击时可留一句**留言**(口癖会自动喊出)。
- 击杀后可以**夺装搜刮**,但击杀者只能取**一件**;通过探索发现尸体的玩家则可反复搜刮。
  空手/只穿内衣的尸体没有可拿的(会提示"放弃了拾取")。
- 枪声与惨鸣会惊动附近的人(15 秒内同区域可见),动手前想清楚。

### 5. 物品

- 背包 5 格 + 饰品 1 格;武器/身体/头/腕/足五个装备位。
- 可使用、丢弃、整理;**合成**:把两件素材合成新装备(配方自行摸索)。
- 弹药类(枪/弓)打完就是烧火棍,记得捡子弹/箭。

### 6. 特殊行动

| 行动 | 条件 | 效果 |
|---|---|---|
| 投毒 | 持有「毒药」 | 往食物(面包/水等)里下毒,吃了扣体力 |
| 验毒 | **料理研究部**专属,耗 30 耐力 | 检查食物是否含毒 |
| 扩音器 | 持有「携带式扩音器」 | 全岛广播一句话(30 秒内全员可见) |
| 黑客入侵 | 持有「笔记本电脑」(有电) | 尝试解除禁区;失败小概率颈环引爆,成功仅维持到次日 0 点 |
| 雷达 | 持有对应道具 | 探知各区域人数分布 |

### 7. 禁区(游戏节奏)

- 每天 **0:00** 追加 3 个禁区(含提示"下次禁止的地区")。
- 滞留禁区的存活玩家在次日 0:00 被**处决**——含解除期间溜进去的人。
- 分校开局即是禁区,想回去必须先黑客解除。
- 禁区推进到第 4 天(ar > 10)后停止新注册;地图会被逐步逼到无路可走。

### 8. 结局

- **优胜**:只剩最后 1 名生存者,且游戏进行到第 5 天起(ar > 10)才判定。
- **EX 全员逃生**:黑客解除禁区 → 回分校击败班主任 → 夺取「程序解除钥匙」→
  在分校使用 → **全员(生存者)逃生**,游戏结束。想全员活命就走这条线。
- 全员死亡则无优胜者,直接落幕。

### 9. 死亡之后

- 立即变为尸体视角:死因、遗言展示;尸体与随身物品留在原地供人搜刮。
- 2 小时后可用同一 ID 重新注册新角色(如果报名还没截止)。

---

## 三、常见问题

**Q:登录提示"尝试次数过多"?**
密码连错 5 次会锁定 10 分钟,等一会儿再试。

**Q:管理后台登录被拒?**
确认启动时设置了 `BR_ADMIN_PASS`;若报"未设置管理密码",说明服务进程没有该环境变量。

**Q:睡眠/治疗好久没恢复?**
恢复在下一次**主动操作**(发命令或重新登录)时结算——轮询页面不算。睡一觉后点任意命令即可看到结算日志。

**Q:换新的一局怎么做?**
管理后台再点一次「数据初始化」即可,旧局数据保留在库中可查。

**Q:公网部署安全吗?**
基础防护已具备(密码哈希、登录锁定、参数化 SQL、转义);公网请务必:
自定义强管理密码、置于 HTTPS 反代之后并设 `BR_COOKIE_SECURE=1`、用反代做 IP 级限速。
